# We Moved

### Installation
`npm install we-moved`

### Usage Instructions
```
const startApp = require('we-moved')

const config = {
  pageTitle: 'We Moved!',
  appName: 'My app name'
}

startApp(config)
```
